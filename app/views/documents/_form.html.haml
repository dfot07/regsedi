= simple_form_for(@document) do |f|
  = f.error_notification

  .form-inputs
    = f.association :user
    = f.association :act
    = f.input :notaria
    = f.input :canton
    = f.input :fecha_escritura

  .form-actions
    = f.button :submit
